[project]
name = "example-repo"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "requests==2.31.0",
    "pydantic==1.10.14",
    "click>=8.0,<9",
]

[project.optional-dependencies]
dev = ["pytest>=7.0", "ruff==0.4.0"]
